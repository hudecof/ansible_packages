---
packages_tasks:
  - debian-config.yml
  - debian-repositories.yml
  - debian-packages.yml

packages_repositories:
  - { src: 'debian-6.list', dest: 'debian.list' }

packages_configs:
  - { src: 'Debian/apt-99local', dest: '99local' }

packages_pkgs:
  - apt
  - aptitude
  - dpkg
  - python-apt
  - sudo
  - debian-archive-keyring
